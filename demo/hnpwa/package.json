{
  "name": "tempo-demo.hnpwa",
  "title": "Hacker News Progressive Web App",
  "description": "Client for Hacker New built using Tempo. Heavily inspired on the Elm implementation.",
  "priority": 10,
  "version": "1.0.0",
  "license": "Apache-2.0",
  "private": true,
  "dependencies": {
    "tempo-std": "0.3.0",
    "tempo-core": "1.7.0",
    "tempo-dom": "0.15.0",
    "tempo-store": "1.2.0",
    "partsing": "0.7.3",
    "dompurify": "2.0.7"
  },
  "devDependencies": {
    "cssnano": "4.1.10",
    "parcel-bundler": "1.12.4",
    "prettier": "1.19.1",
    "tslint": "6.0.0",
    "typescript": "3.7.5",
    "babel-types": "6.26.0",
    "sass": "1.23.7",
    "@types/dompurify": "2.0.0"
  },
  "scripts": {
    "compile": "NODE_ENV=production && npx tsc --noEmit && parcel build src/index.html --public-url . --out-dir ./build --experimental-scope-hoisting",
    "clean": "rm -rf dist/* && rm -rf ./build/*",
    "build": "npm run clean && npm run compile",
    "watch": "parcel src/index.html",
    "tslint": "tslint -p ./tslint.json",
    "tslint-fix": "tslint -p ./tslint.json --fix",
    "prettier": "prettier -c src/**/*.ts",
    "prettier-fix": "prettier --write src/**/*.ts"
  }
}
